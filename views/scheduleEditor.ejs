<%-include('templates/header');%>
    <%  //import the database array and store it as a local array to be added/deleted to 
        //could update database in real time 
        //when removing from list, 
    var exercises; 
    for (let day in workouts[0]) {
        exercises = workouts[0][day];
        for (let j = 0; j < workouts[0][day].length; j++) { %>
            <li>
                <%= workouts[0][day][j] %>
            </li>
        <% }
        } %>
        <br>

        <!-- will need to create new post method for this in index.js
            <div class="col-md-4">
            <form action="/scheduleEditor" method="post" class="d-flex">
                <div class="dropdown me-2">
                    <button class="btn btn-secondary dropdown-toggle" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Difficulty
                    </button>
                    <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
                        <a class="dropdown-item" href="/filtering/beginner">Beginner</a>
                        <a class="dropdown-item" href="/filtering/intermediate">Intermediate</a>
                        <a class="dropdown-item" href="/filtering/expert">Expert</a>
                    </div>
                </div>
                <input name="search" id="searchbar" class="form-control me-2"
                    type="search" placeholder="Search" aria-label="Search" value="<%= searchParam %>">
                    <input type="hidden" name="filter" value="<%= filter %>">
                <button class="btn btn-primary" type="submit">Submit</button>
            </form>
        </div> -->

        <!-- workouts listed -->
        <ul class="list-unstyled">
        <% exercisesInfo.forEach(exercise => { %>
            <li id="<%= exercise.name %>" class="mb-3 ">
                <a href="/exercise/<%= exercise.id %>" class="text-decoration-none">
                    <div class="card">
                        <div class="card-body">
                            <!-- compare this workout to all workouts in local array and if match found, set check to true 
                                use name= "selectedWorkout" to do req.body stuff -->
                            <!-- Credit: ChatGPT - helped with checkmarking workouts that are already in your workout list -->
                            <input type="checkbox" <%= exercises.includes(exercise.name) ? 'checked' : '' %>>
                            <h5 class="card-title"><%= exercise.name %></h5>
                            <p class="card-text">Muscles: <%= exercise.primaryMuscles[0].charAt(0).toUpperCase() + exercise.primaryMuscles[0].slice(1) %><br>Difficulty: <%=exercise.level.charAt(0).toUpperCase() + exercise.level.slice(1) %></p>
                        </div>
                    </div>
                </a>
            </li>
        <% }); %>
    </ul>

    <!-- pagination -->
    <nav aria-label="Page navigation">
        <ul class="pagination justify-content-center">
            <li class="page-item <%= currentPage === 1 ? 'disabled' : '' %>">
                <a class="page-link" href="?filter=<%= filter %>&search=<%= searchParam %>&page=1" tabindex="-1" aria-disabled="true">First</a>
            </li>
            <% for(let page = Math.max(1, currentPage - 2); page <= Math.min(totalPages, currentPage + 2); page++) { %>
                <li class="page-item <%= page === currentPage ? 'active' : '' %>">
                    <a class="page-link" href="?filter=<%= filter %>&search=<%= searchParam %>&page=<%= page %>"><%= page %></a>
                </li>
            <% } %>
            <li class="page-item <%= currentPage === totalPages ? 'disabled' : '' %>">
                <a class="page-link" href="?filter=<%= filter %>&search=<%= searchParam %>&page=<%= totalPages %>" tabindex="-1" aria-disabled="true">Last</a>
            </li>
        </ul>
    </nav>
    <form action="/scheduleSave">
        <button type="button" name="btn button-success">Save</button>
    </form>
    <button type="button" class="btn button-outline-light">
        <a href="/schedule">Cancel</a>
    </button>
<%-include('templates/footer');%>